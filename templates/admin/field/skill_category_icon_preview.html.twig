{% if field.value %}
    <twig:ux:icon name="{{ field.value }}" height="20" width="20" class="me-2 fs-4" />
    <small class="text-muted">{{ field.value }}</small>
{% else %}
    <span class="text-muted">No icon</span>
{% endif %}
