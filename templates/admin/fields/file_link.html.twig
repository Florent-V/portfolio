{% if field.value %}
    <a href="{{ asset(field.customOptions.get('base_path') ~ field.value) }}" target="_blank">
        {{ field.value }} (Download)
    </a>
{% else %}
    <span class="text-muted">No file uploaded.</span>
{% endif %}
